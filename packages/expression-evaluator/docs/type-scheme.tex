\documentclass{article}
\usepackage[utf8]{inputenc}

% Page setup
\usepackage[a4paper,landscape,margin=2cm]{geometry}
\usepackage{amsmath}

% Typography
\usepackage[scaled]{helvet}
\let\familydefault\sfdefault

\usepackage[usenames,svgnames]{xcolor}
\usepackage{tikz,pgfplots}
\usetikzlibrary{positioning,arrows,intersections,calc}

% Set page size to tikz figure size
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{5pt}

\definecolor{coloractor}    {RGB}{199,212,104}
\definecolor{colormediator} {RGB}{ 79,142,209}
\definecolor{colorbus}      {RGB}{143,232,186}
\definecolor{colortext}     {RGB}{ 29, 29, 27}
\definecolor{coloraction}   {RGB}{129, 29, 27}
\definecolor{colortest}     {RGB}{129,129,227}

\begin{document}
	\pagestyle{empty}
	\begin{tikzpicture}[
		node distance = 2em, auto,
		font={\Large\itshape},
		base/.style={text=colortext,font={\Large\bfseries},inner sep=10pt,align=center,rectangle},
		relation/.style={text width=13em},
		typeUrl/.style={base,fill=coloractor,text width=15em},
		typeSparql/.style={base,fill=colorbus,text width=15em},
		]
		
		% Legend
		\draw [fill=black!1!white] (-43,0) rectangle (-30,2);
		\node[typeUrl] at (-40,1) {Type with url};
		\node[typeSparql] at (-33,1) {Internal SPARQL type};
		
		% base type
		\node[typeSparql] (term) {Term};
		
		% All types based on base
		
	
		\node[typeSparql,below= 3 of term] (numeric) {numeric};
		\draw[->,thick](term.south) -- (numeric.north);
			% ==== numeric types ====
			
			\node[typeUrl, below=of numeric] (double) {double};
			\draw[->, thick](numeric.south) -- (double.north);
			
			\node[typeUrl, left=of double] (float) {float};
			\draw[->, thick](numeric.south) -- (float.north);
			
			
			\node[typeUrl, right=of double] (decimal) {decimal};
			\draw[->, thick](numeric.south) -- (decimal.north);
				% ==== decimal Types ====
				\node[typeUrl, below=of decimal] (integer) {integer};
				\draw[->,thick](decimal.south) -- (integer.north);
				
				\node[typeUrl, below=of integer] (nonPositiveInteger) {nonPositiveInteger};
				\draw[->,thick](integer.south) -- (nonPositiveInteger.north);
				\node[typeUrl, below=of nonPositiveInteger] (negativeInteger) {negativeInteger};
				\draw[->,thick](nonPositiveInteger.south) -- (negativeInteger.north);
				
				\node[typeUrl, right=of nonPositiveInteger] (nonNegativeInteger) {nonNegativeInteger};
				\draw[->,thick](integer.south) -- (nonNegativeInteger.north);
				\node[typeUrl, below=of nonNegativeInteger] (positiveInteger) {positiveInteger};
				\draw[->,thick](nonNegativeInteger.south) -- (positiveInteger.north);
				
				\node[typeUrl, right=of positiveInteger] (unsignedLong) {unsignedLong};
				\draw[->,thick](nonNegativeInteger.south) -- (unsignedLong.north);
				\node[typeUrl, below=of unsignedLong] (unsignedInt) {unsignedInt};
				\draw[->,thick](unsignedLong.south) -- (unsignedInt.north);
				\node[typeUrl, below=of unsignedInt] (unsignedShort) {unsignedShort};
				\draw[->,thick](unsignedInt.south) -- (unsignedShort.north);
				\node[typeUrl, below=of unsignedShort] (unsignedByte) {unsignedByte};
				\draw[->,thick](unsignedShort.south) -- (unsignedByte.north);
				
				\node[typeUrl, left=of nonPositiveInteger] (long) {long};
				\draw[->,thick](integer.south) -- (long.north);
				\node[typeUrl, below=of long] (int) {int};
				\draw[->,thick](long.south) -- (int.north);
				\node[typeUrl, below=of int] (short) {short};
				\draw[->,thick](int.south) -- (short.north);
				\node[typeUrl, below=of short] (byte) {byte};
				\draw[->,thick](short.south) -- (byte.north);
				
				% ========
			
			% ========
		
		% Left part
		
		\node[typeUrl,left=of numeric] (dateTime) {dateTime};
		\draw[->,thick](term.south) -- (dateTime.north);
		
		\node[typeUrl, left=of dateTime] (anyURI) {anyURI};
		\draw[->, thick](term.south) -- (anyURI.north);

		\node[typeUrl, left=of anyURI] (base64) {base64Binary};
		\draw[->, thick](term.south) -- (base64.north);

		\node[typeUrl, left=of base64] (boolean) {boolean};
		\draw[->, thick](term.south) -- (boolean.north);		
		
		\node[typeSparql,left=of boolean] (stringly) {stringly};
		\draw[->,thick](term.south) -- (stringly.north);
			% ==== stringly Types ====
			\node[typeUrl, below=of stringly] (langString) {langString};
			\draw[->, thick](stringly.south) -- (langString.north);
			
			\node[typeUrl, right=of langString] (string) {string};
			\draw[->, thick](stringly.south) -- (string.north);
				% ==== string Types ====
				\node[typeUrl, below=of string] (normalizedString) {normalizedString};
				\draw[->,thick](string.south) -- (normalizedString.north);
				\node[typeUrl, below=of normalizedString] (token) {token};
				\draw[->,thick](normalizedString.south) -- (token.north);
				\node[typeUrl, below=of token] (language) {language};
				\draw[->,thick](token.south) -- (language.north);
				\node[typeUrl, right=of language] (NMTOKEN) {NMTOKEN};
				\draw[->,thick](token.south) -- (NMTOKEN.north);
				\node[typeUrl, left=of language] (Name) {Name};
				\draw[->,thick](token.south) -- (Name.north);
				\node[typeUrl, below=of Name] (NCName) {NCName};
				\draw[->,thick](Name.south) -- (NCName.north);
				\node[typeUrl, below=of NCName] (ID) {ID};
				\draw[->,thick](NCName.south) -- (ID.north);
				\node[typeUrl, left=of ID] (ENTITY) {ENTITY};
				\draw[->,thick](NCName.south) -- (ENTITY.north);
				\node[typeUrl, right=of ID] (IDREF) {IDREF};
				\draw[->,thick](NCName.south) -- (IDREF.north);
				
				% ========
			
			% ========
		
		\node[typeUrl, left=of stringly] (date) {date};
		\draw[->, thick](term.south) -- (date.north);	
		
		\node[typeUrl, left=of date] (dateTime) {dateTime};
		\draw[->, thick](term.south) -- (dateTime.north);
			% ==== dateTime Types ====	
			\node[typeUrl, below=of dateTime] (dateTimeStamp) {dateTimeStamp};
			\draw[->,thick](dateTime.south) -- (dateTimeStamp.north);
			
			% ========
		
		\node[typeUrl, left=of dateTime] (gDay) {gDay};
		\draw[->, thick](term.south) -- (gDay.north);
		
		\node[typeSparql,left=of gDay] (other) {other};
		\draw[->,thick](term.south) -- (other.north);
		
		% Right part
		
		\node[typeUrl, right=of numeric] (gMonth) {gMonth};
		\draw[->, thick](term.south) -- (gMonth.north);	
		
		\node[typeUrl, right=of gMonth] (gMonthDay) {gMonthDay};
		\draw[->, thick](term.south) -- (gMonthDay.north);	
		
		\node[typeUrl, right=of gMonthDay] (gYear) {gYear};
		\draw[->, thick](term.south) -- (gYear.north);	
		
		\node[typeUrl, right=of gYear] (gYearMonth) {gYearMonth};
		\draw[->, thick](term.south) -- (gYearMonth.north);	
		
		\node[typeUrl, right=of gYearMonth] (hexBinary) {hexBinary};
		\draw[->, thick](term.south) -- (hexBinary.north);	
		
		\node[typeUrl, right=of hexBinary] (NOTATION) {NOTATION};
		\draw[->, thick](term.south) -- (NOTATION.north);
		
		\node[typeUrl, right=of NOTATION] (duration) {duration};
		\draw[->, thick](term.south) -- (duration.north);
			% ==== duration Types ====
			\node[typeUrl, below=of duration] (dayTimeDuration) {dayTimeDuration};
			\draw[->,thick](duration.south) -- (dayTimeDuration.north);
			
			\node[typeUrl, right=of dayTimeDuration] (yearMonthDuration) {yearMonthDuration};
			\draw[->,thick](duration.south) -- (yearMonthDuration.north);
			
			% ========
		
		\node[typeUrl, right=of duration] (QName) {QName};
		\draw[->, thick](term.south) -- (QName.north);	
		
		\node[typeUrl, right=of QName] (time) {time};
		\draw[->, thick](term.south) -- (time.north);
		
		\node[typeSparql,right=of time] (nonLexical) {nonLexical};
		\draw[->,thick](term.south) -- (nonLexical.north);	
		
	\end{tikzpicture}
\end{document}