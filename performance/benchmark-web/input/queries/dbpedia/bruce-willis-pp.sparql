# Spouses and children of Bruce Willis
# Datasource: https://fragments.dbpedia.org/2016-04/en
SELECT ?person
WHERE {
  [ rdfs:label "Bruce Willis"@en ]
    (dbpedia-owl:spouse|dbpedia-owl:child) ?person.
}
