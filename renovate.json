{
  "extends": [
    "github>rubensworks/renovate-presets:js"
  ],
  "packageRules": [
    {
      "description": "Agentic workflow lock files are generated by 'gh aw compile'. Bumping the pins inside them leaves the lock file out of sync with the compiler that produced it, which breaks the workflow at runtime (#1770). Regenerate them with 'gh aw compile' instead.",
      "matchFileNames": [
        ".github/workflows/*.lock.yml"
      ],
      "enabled": false
    }
  ]
}
